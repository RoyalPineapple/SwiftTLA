name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4
      - run: swift test --filter SwiftTLATests

  macro:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4
      - run: swift build --target SwiftTLAPlugin
      - run: swift build --target SwiftTLAExamples
